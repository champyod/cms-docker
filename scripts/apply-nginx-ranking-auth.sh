#!/bin/bash
# [deprecated] nginx ranking basic-auth helper
# now handled by contest-stack nginx config (config/nginx-contest.conf RANKING_AUTH_DIRECTIVES)
echo "[deprecated] nginx ranking auth — now handled by contest-stack nginx config (config/nginx-contest.conf RANKING_AUTH_DIRECTIVES); see docs/TUTORIAL.md" >&2
exit 0
